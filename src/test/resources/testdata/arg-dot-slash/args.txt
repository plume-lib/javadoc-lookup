# The argument is not a normalized file name.  The goal file records that the references
# are normalized ("file:api/...") but the ignored prefix is not ("file:./api/"), so the
# ignored prefix matches none of the references.
./api/index-all.html
